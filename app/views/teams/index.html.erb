<div class="col">
  <% @teams.each do |team| %>
      <%= image_tag url_for(team.image), class: 'img-thumbnail', width: '25%' %>
      <%= link_to team.name, team, class: 'btn btn-link' %>
  <% end %>
</div>

<%== pagy_bootstrap_nav(@pagy) %>