<%= form_with(url: search_results_teams_path, method: "get", local: true) do %>
  <%= label_tag(:query, "Search for:") %>
  <%= text_field_tag(:query) %>
  <%= submit_tag("Search") %>
<% end %>

<%= render 'layouts/alerts' %>

<div class="card-deck">
  <%= render partial: 'team' %>
</div>

<%= pagy_bootstrap_nav(@pagy).html_safe %>